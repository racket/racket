
AC_ARG_ENABLE(pkgs,         [  --enable-pkgs=<pkgs>    add <pkgs> on install])
AC_ARG_ENABLE(missingpkgs,  [  --enable-missingpkgs    add missing package dependencies on install])
AC_ARG_ENABLE(catalog,      [  --enable-catalog=<url>  install packages from <url>])
