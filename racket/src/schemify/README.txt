The "schemify" compiler takes a linklet and converts it to a `lambda`.
Converting a linklet with schemify is a step to compiling it either
via Chez Scheme or (via the "cify" compiler) to C code to embed in the
Racket virtual machine.

See "../cs/README.txt" for more information.
