(module info (lib "infotab.ss" "setup")
  (define name "Galore")
  (define blurb
    (list "A library of functional data structures"))
  (define primary-file "[file]")
  (define doc.txt "doc.txt")
  (define categories '(datastructures))
  (define version "2.1"))
