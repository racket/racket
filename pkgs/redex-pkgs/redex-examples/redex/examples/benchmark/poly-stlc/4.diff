12c12
< (define the-error "no error")
---
> (define the-error "the type of cons is incorrect")
120c120
<    (∀ a (a → ((list a) → (list a))))]
---
>    (∀ a (a → ((list a) → a)))]
262c262
<       M]
---
>      M]
