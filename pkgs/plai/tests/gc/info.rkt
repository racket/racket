#lang info

(define compile-omit-paths '("bad-mutators"
                             "bad-collectors"
                             "other-mutators"))
(define test-omit-paths '("bad-mutators"
                          "other-mutators"))
