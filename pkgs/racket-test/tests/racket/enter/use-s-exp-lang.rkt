#lang s-exp "reexport-racket.rkt"

;; This module is used to check that `enter!` is able to
;; find the `s-exp` reader, which is implemented in the
;; onld "lang/reader.rkt" way and not the submodule way.
